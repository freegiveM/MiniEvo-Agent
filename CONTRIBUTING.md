# 参与开发

## 环境

Python 3.11（硬下限，理由见 `pyproject.toml`）。

```bash
git clone <your-fork-url> && cd MiniEvo-Agent
python -m pip install -r requirements.txt
python -m pytest -q
```

全套测试**不需要 LLM、不需要网络、不需要 API key**，也不该需要——任何要
真实调用才能跑的东西都用 stub 覆盖。如果你新加的测试要联网才能过，那不是
测试，是探测脚本，它应该放到 `scripts/` 下并且有 `--db` 之类的显式开关。

跑起服务的方式见 README 的「快速开始」。

## 提交前

```bash
python -m pytest -q
```

必须全绿。这个仓库现在的基线是 876 passed。

## 三条与这个项目关系特别大的约定

### 1. 声称"能用"要分等级

README 是按**证据等级**分组的，不是按功能分组：「在评测链路上，有量化
结论」／「有单测但不在评测链路上」／「写了但跑不到」。加了新东西就加到
它实际所属的那一档，不要往第一档里塞。理由很直接：一份能力清单里最容易
骗人的地方不是某句话假，而是跑过评测的和只写完了的混在一起列，读者没法
区分。

### 2. 门禁不能假装在工作

这是这个项目反复踩到的同一个坑，值得单独写一条：**一道假装通过的门禁比
一道不存在的门禁更危险。**几个真实例子——

- 某个指标的分母是 0 时，非退化检查会以"从没测过，无从退化"为由放行。
  分母是 1 更阴险：它会一直打印一个看起来很正常的 0.0/1.0，而那个数字
  不携带任何信息。
- 报告不是门禁。曾经有一节文档精确诊断了 token 预算不足并写好了报错文案，
  但**预算本身没改**——于是那条正确的报错忠实地报告了一个从未被修的阻塞，
  而对应的回路一次都没真跑起来过。
- 这个代码库里 `None` 一律表示"没跑到"。所以一个门禁键名拼错，会被读成
  "这道门禁没执行"而静默通过。

改动任何门禁、任何指标分母、任何 `gates` 键名时，先问：它失败的时候，
**看起来**像失败吗？

### 3. 人判的数据和脚本产物要分清

- `datasets/` 下入库的是「怎么得到它」（采集器 + `repos.yaml` + 筛选
  规则），不是产物。例外只有两类：变异测试集（输入是本仓库源码，每次
  提交都在变，必须钉住才能让不同时间点的数字可比）和**人工标注文件**
  （label 是人判的，重跑任何脚本都生成不出来）。
- 模型生成的标注**不得**写进 `datasets/` 或主库，只写到 `output/` 下的
  副本，且每条 note 带 `[model-labelled]` 前缀，以便整批识别与撤除。
- 反馈进入进化回路必须过 `HUMAN_CONFIRMED_CATEGORIES` 白名单
  （`evolution.py`）。用白名单而不是黑名单是有意的：新增一个推断类别时，
  默认是被排除，而不是默认被信任。

## 长跑任务逐条落盘

任何昂贵的顺序批处理（LLM 批量标注、全量 replay、多轮进化探测）必须
**逐条 append + `flush()` + `os.fsync()`**，不能等跑完再一次性写。跑到
第 90 条崩掉然后从头重来，代价是真金白银。崩溃分支也要落盘——崩溃本身
就是那次运行的结果之一。

## 行尾

仓库有 `.gitattributes` 强制 LF。别绕过它：`skills/*/skill.json` 里的
sha256 是对 `skill.py` 逐字节算的，行尾一变校验就失败，而报错指向
校验和，看不出根因。

## 密钥

`.env` 已被 gitignore，且从未进过任何一次提交。不要提交任何真实密钥；
`.env.example` 里只放占位符。