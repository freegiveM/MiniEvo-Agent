"""原型代码：能跑，但**不在评测链路上**，所以没有数字支撑。

这个目录与 evoagent/ 的区别不是"好坏"或"新旧"，是**有没有被量过**：

  evoagent/     —— 评测跑的就是这里的代码。报出来的每个数
                   （recall / 有效告警率 / 消融差值）都来自这条路径。
  prototypes/   —— 实现完整、测试也是绿的，但评测从没经过它。
                   它的行为没有任何量化结论。

为什么不直接删：里面的东西确实被 service.py 的某条分支调用着，删了
会改变线上行为；而且协作协议本身是想清楚过的，不是废稿。

为什么不留在 evoagent/：留着的话，"这个项目做了多智能体协作"这句话
就有两个互相竞争的指代对象，而只有一个有数据。面试里被问"你说的
多智能体是哪套、提升多少"，答不上来的那套会把答得上来的那套一起拖下水。

判断标准：**一段代码要么进评测，要么进 prototypes/。**
"实现了但没量过"不该和"量过并且有结论"放在同一个目录里。
"""
