__pycache__/
*.py[cod]
*.db
.env
.pytest_cache/


# 评测/进化运行产物：每次跑都会重生成，不入库
output/
datasets/*.db

# 数据集本体与抓取缓存不入库：几十 MB 的 diff，且可由 repos.yaml 完全复现。
# 入库的是**采集器 + 仓库清单 + 筛选规则**（即"怎么得到它"），
# 而不是产物——产物入库反而会让人误以为数据集是手工整理的。
datasets/*.jsonl
datasets/.diff-cache/

# 变异测试集**例外，入库**。和上面的理由不冲突，是同一条原则的另一面：
# 反转修复集的输入（repos.yaml + 采集器）是稳定的，所以产物可复现，不必入库。
# 变异集的输入是**本仓库自己的源码**，它每次提交都在变——同一条命令下个月
# 跑出来就是另一批样本。要让不同时间点的评测数字可比，必须把这批样本钉住。
!datasets/mutation-v1.jsonl

# 标注文件**也入库**，理由比变异集更强：里面的 label 是人判的，
# 重跑任何脚本都生成不出来。丢了就得从头标一遍，而且重标的结果与
# 原来的不是同一批数据（rubric 会漂）。
!datasets/labelling-*.json
